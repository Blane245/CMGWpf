<Window x:Class="CMGWpf.Dialogs.RenameTrack"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMGWpf.Dialogs"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:viewmodel="clr-namespace:CMGWpf.View"
        d:DataContext="{d:DesignInstance Type=viewmodel:TrackViewModel}"
        Title="Rename Track"
        mc:Ignorable="d"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        Closing="Window_Closing">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Border Height="20" VerticalAlignment="Top">
                <Grid Background="LightBlue">
                    <TextBlock 
                    Text="Rename Track" 
                    Foreground="Black" 
                    FontSize="12" 
                    FontWeight="Bold" 
                    VerticalAlignment="Center" 
                    HorizontalAlignment="Left" 
                    Margin="5,0,0,0"/>
                    <Button x:Name="CloseDialog" Width="20" Height="15" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,3,5,0" Content="X" FontSize="10"
                    Command="{Binding RenameCancelCommand}"
                    />
                </Grid>
            </Border>
            <StackPanel Orientation="Horizontal" Margin="2">
                <Label Content="Current Track Name:"/>
                <Label Content="{Binding Track.Name}" HorizontalAlignment="Left" Margin="5" VerticalAlignment="Center"/>
                <Label Content="New Track Name:"/>
                <TextBox Text="{Binding NewTrackName, UpdateSourceTrigger=PropertyChanged}" Margin="5"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="2">
                <Button Grid.Column="0" Grid.Row="1" x:Name="OK" Width="40" Height="20" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10,0,0,10" Content="OK" Command="{Binding RenameOKCommand}"/>
                <Button Grid.Column="0" Grid.Row="1"  x:Name="Cancel" Width="40" Height="20" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="10,0,0,10"  Content="Cancel" Command="{Binding RenameCancelCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
