<UserControl x:Class="CMGWpf.Panels.Tools.GeneratorsAlignPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CMGWpf.Panels.Tools"
                     xmlns:viewmodel="clr-namespace:CMGWpf.View"
                     d:DataContext="{d:DesignInstance Type=viewmodel:ToolsViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical">
        <Label Content="Align Generators Start or Stop Time" FontSize="16"/>
        <StackPanel Orientation="Horizontal">
            <Label Content="Primary Generator: "/>
            <ComboBox ItemsSource="{Binding GeneratorList}" SelectedItem="{Binding PrimaryGeneratorName, Mode=TwoWay}" ToolTip="Select the generator that will be used to determine whether start or stop time is to aligned."/>
            <Label Content="Secondary Generators: "/>
            <ListBox x:Name="SecondaryGeneratorsListBox"
                             ItemsSource="{Binding GeneratorList}" 
                             SelectionMode="Multiple" 
                     Height="150"
                             ToolTip="Select the generators whose start or stop time will be aligned."/>
            <Label Content="Maintain Time? "/>
            <ComboBox ItemsSource="{Binding MaintainAlignTimeOptions}" SelectedItem="{Binding MaintainTimeOption}" ToolTip="Select either the start or stop time for setting generation durations equal."/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Width="50" Content="Align" Command="{Binding Align}" Margin="5"/>
        </StackPanel>
    </StackPanel>
</UserControl>
