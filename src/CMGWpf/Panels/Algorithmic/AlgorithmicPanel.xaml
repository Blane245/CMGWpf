<UserControl x:Class="CMGWpf.Panels.Algorithmic.AlgorithmicPanel"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
 xmlns:services="clr-namespace:CMGWpf.Services"
 xmlns:viewmodel="clr-namespace:CMGWpf.View"
 d:DataContext="{d:DesignInstance Type=viewmodel:GeneratorViewModel}"
 xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
 xmlns:local="clr-namespace:CMGWpf.Panels.Algorithmic"
 xmlns:model="clr-namespace:CMGWpf.Model"
 mc:Ignorable="d" 
 d:DesignHeight="600" d:DesignWidth="1500">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                <Label Content="SoundFont:"/>
                <ComboBox ItemsSource="{Binding SoundFontFileNames}" SelectedValue="{Binding AlgorithmicSoundFontFileName, Mode=TwoWay}" ToolTip="Select a SoundFont file" />
                <Label Content="Preset:"/>
                <ComboBox ItemsSource="{Binding AlgorithmicPresetNames}" SelectedItem="{Binding AlgorithmicPresetName, Mode=TwoWay}" ToolTip="Select a preset from the chosen SoundFont" />
            </StackPanel>
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                <Label Content="Looping?:"/>
            <CheckBox Content="" IsChecked="{Binding AlgorithmicGenerator.IsLooping}" ToolTip="Enable or disable looping for the algorithmic generator" />
            <Label Content="Attack?:"/>
            <CheckBox Content=""  IsChecked="{Binding AlgorithmicGenerator.AttackEnabled}" ToolTip="Enable or disable attack for the algorithmic generator" />
            <Label Content="Microtones?:"/>
            <CheckBox Content=""  IsChecked="{Binding AlgorithmicGenerator.Microtones}" ToolTip="Enable or disable microtones for the algorithmic generator" />
            </StackPanel>
            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
                <Label Content="Noise:"/>
                <Label Content="Seed:"/>
                <TextBox TextWrapping="NoWrap" Text="{Binding AlgorithmicGenerator.NoiseSeed}" ToolTip="Enter the seed for the noise generator" />
                <Button Content="New Seed" Background="Green" Foreground="White" Command="{Binding NoiseSeedCommand}" CommandParameter="{Binding AlgorithmicGenerator}" ToolTip="Generate a new seed for the noise generator"/>
                <Label Content="Level (+-10dB):"/>
                <xctk:DoubleUpDown  Margin="5,0,0,0" Minimum="-10" Maximum="10" Increment="1" Value="{Binding AlgorithmicGenerator.NoiseAmplitude}" ToolTip="Set the amplitude level for the noise generator relative to the current volume"/>
                <Label Content="Frequency (Hz):"/>
                <xctk:DoubleUpDown Margin="5,0,0,0" Minimum="0" Maximum="1000" Increment="0.001" Value="{Binding AlgorithmicGenerator.NoiseFrequency}" ToolTip="Set the frequency (Hz) for the noise generator"/>
            </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                <Label Content="Reverb:" FontStyle="Italic"/>
                <Label Content="Delay (msec)"/>
                <xctk:DoubleUpDown Margin="5,0,0,0" Minimum="0" Maximum="10000" Increment="1" Value="{Binding AlgorithmicGenerator.ReverbDelay}" ToolTip="Set the delay time (msec) for the reverb effect. Zero means no reverb"/>
                <Label Content="Decay (1-10 dB)"/>
                <xctk:IntegerUpDown Margin="5,0,0,0" Minimum="1" Maximum="10" Value="{Binding AlgorithmicGenerator.ReverbDecay}" ToolTip="Set the decay level for the reverb effect"/>
            </StackPanel>

        <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
                <Label Content="Tremolo:" FontStyle="Italic"/>
                <Label Content="Speed (mHz)"/>
                <xctk:DoubleUpDown Margin="5,0,0,0" Minimum="0" Maximum="100000" Increment="1" Value="{Binding AlgorithmicGenerator.Tremolo.Speed}" ToolTip="Set the speed for the tremolo effect"/>
                <Label Content="Depth (dB)"/>
                <xctk:DoubleUpDown Margin="5,0,0,0" Minimum="0" Maximum="10" Increment="0.1" Value="{Binding AlgorithmicGenerator.Tremolo.Depth}" ToolTip="Set the depth for the tremolo effect"/>
                <Label Content="Modulator"/>
                <ComboBox ItemsSource="{Binding ModulatorTypes}" SelectedItem="{Binding AlgorithmicGenerator.Tremolo.WaveForm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" IsSynchronizedWithCurrentItem="True" ToolTip="Select the modulator waveform for the tremolo effect"/>
            </StackPanel>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal">
                <Label Content="Vibrato:" FontStyle="Italic"/>
                <Label Content="Speed (Mhz)"/>
                <xctk:DoubleUpDown Margin="5,0,0,0" Minimum="0" Maximum="100000" Increment="1" Value="{Binding AlgorithmicGenerator.Vibrato.Speed}" ToolTip="Set the speed for the vibrato effect"/>
                <Label Content="Depth (cents)"/>
                <xctk:DoubleUpDown Margin="5,0,0,0" Minimum="0" Maximum="10000" Increment="0.1" Value="{Binding AlgorithmicGenerator.Vibrato.Depth}" ToolTip="Set the depth for the vibrato effect"/>
                <Label Content="Modulator"/>
                <ComboBox ItemsSource="{Binding ModulatorTypes}" SelectedItem="{Binding AlgorithmicGenerator.Vibrato.WaveForm, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ToolTip="Select the modulator waveform for the vibrato effect"/>
            </StackPanel>
            <StackPanel Grid.Row="3" Grid.Column="0"  Orientation="Horizontal">
                <Label Content="Measure:" FontStyle="Italic"/>
                <Label Content="Length (beats)"/>
                <xctk:IntegerUpDown Minimum="2" Maximum="100" Increment="1" Value="{Binding AlgorithmicGenerator.MeasureLength}" ToolTip="Set the length of the measure in beats"/>
                <Label Content="On Beats (beats)"/>
            <xctk:IntegerUpDown Minimum="2" Maximum="{Binding AlgorithmicGenerator.MeasureLength}" Increment="1" Value="{Binding AlgorithmicGenerator.BeatCount}" ToolTip="Set the number of beats that are active in the measure [1 - Measure Length]"/>
            <Label Content="Beat Shift (beats)"/>
                <xctk:IntegerUpDown Minimum="0" Maximum="{Binding AlgorithmicGenerator.MeasureLength}" Increment="1" Value="{Binding AlgorithmicGenerator.OffsetSequence}" ToolTip="Set the beat shift for the active notes of the measure"/>
            </StackPanel>
            <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
                <Label Content="Octave:" FontStyle="Italic"/>
                <Label Content="Note Count (1-12)"/>
                <xctk:IntegerUpDown Minimum="1" Maximum="12" Increment="1" Value="{Binding AlgorithmicGenerator.NoteCount}" ToolTip="Set the number of active notes in the octave"/>
                <Label Content="Shift (tones)"/>
                <xctk:IntegerUpDown Minimum="1" Maximum="11" Increment="1" Value="{Binding AlgorithmicGenerator.OffsetNotes}" ToolTip="Set the note shift of the active octave notes"/>
            </StackPanel>
        <Border Grid.Row="4" Grid.ColumnSpan="2" Height="1" Background="Black"/>
        <ScrollViewer Grid.Row="5" Grid.ColumnSpan="2"  HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <StackPanel Grid.IsSharedSizeScope="True">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="AttributeNameColumn"/>
                        <ColumnDefinition Width="Auto" SharedSizeGroup="AlgorithmColumn"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Grid.Column="0" Content="Attribute"/>
                    <Label Grid.Column="1" Content="Algorithm"/>
                    <Label Grid.Column="2" Content="Parameters"/>
                </Grid>
                <Border BorderThickness="1" BorderBrush="LightGray"/>
                <local:Attribute DataContext="{Binding NoteAttribute}"/>
                <Border BorderThickness="1" BorderBrush="LightGray" />
                <local:Attribute DataContext="{Binding AttackAttribute}"/>
                <Border BorderThickness="1" BorderBrush="LightGray" />
                <local:Attribute DataContext="{Binding SpeedAttribute}"/>
                <Border BorderThickness="1" BorderBrush="LightGray" />
                <local:Attribute DataContext="{Binding DurationAttribute}"/>
                <Border BorderThickness="1" BorderBrush="LightGray" />
                <local:Attribute DataContext="{Binding VolumeAttribute}"/>
                <Border BorderThickness="1" BorderBrush="LightGray" />
                <local:Attribute DataContext="{Binding PanAttribute}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
