bugs
	playback is terminating at the generator stop time not as the clouds fade away
	play muted voices are appearing in the voices legend. are they being played?
	width of track control icon are too large taking space away from track name
	CMG windows are overlapping task bar
	fix time interval highlighting
testing
	do thorough testing of all generator parameters
	put the DSP through its paces check all controls and algorithms.
implementation
    redesign of user interaction with app
	void DataWindow_Closing(object sender, CancelEventArgs e)
        {
            MessageBox.Show("Closing called");

            // If data is dirty, notify user and ask for a response
            if (this.isDataDirty)
            {
                string msg = "Data is dirty. Close without saving?";
                MessageBoxResult result = 
                  MessageBox.Show(
                    msg, 
                    "Data App", 
                    MessageBoxButton.YesNo, 
                    MessageBoxImage.Warning);
                if (result == MessageBoxResult.No)
                {
                    // If user doesn't want to close, cancel closure
                    e.Cancel = true;
                }
            }
        }
	add timeinterval to the xml
    add input gestures to recent files
	implement reporting
    implement audio recording (and scroll roll movie recording)
	write documentation