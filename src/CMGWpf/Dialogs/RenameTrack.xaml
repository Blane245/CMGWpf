<Window x:Class="CMGWpf.Dialogs.RenameTrack"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMGWpf.Dialogs"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:viewmodel="clr-namespace:CMGWpf.View"
        d:DataContext="{d:DesignInstance Type=viewmodel:TrackViewModel}"
        Title="Rename Track"
        mc:Ignorable="d"
        WindowStyle="None"
        Background="White"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0">
            <Label Grid.Row="0" Grid.Column="0"  Content="Rename Track" Foreground="Black" FontWeight="Bold" VerticalAlignment="Center"/>
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0">
            <Button Width="20" Height="15" HorizontalAlignment="Right" VerticalAlignment="Top" Content="X" Command="{Binding RenameCancelCommand}" Style="{StaticResource WindowButton}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0">
            <Label Content="New Track Name:"/>
            <TextBox Text="{Binding NewTrackName, UpdateSourceTrigger=PropertyChanged}" Width="150" ToolTip="Enter a new unique name for the track."/>
        </StackPanel>
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0" Orientation="Horizontal">
            <Button Width="50" Content="OK" Command="{Binding RenameOKCommand}"/>
            <Button Width="50" Content="Cancel" Command="{Binding RenameCancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
